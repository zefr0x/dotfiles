[user]
    name = zer0-x
    email = zer0-x.7ty50@aleeas.com
[core]
    editor = nvim
[color]
    ui = true
[init]
    defaultBranch = main
[filter "lfs"]
    clean = git-lfs clean -- %f
    smudge = git-lfs smudge -- %f
    process = git-lfs filter-process
    required = true
[diff]
    tool = nvim -d
    external = difft
[status]
    branch = true
    showStash = true
    short = true
[alias]
    ds = diff --staged
    d = diff
    s = status
    a = add
    ap = add --patch
    rs = restore --staged
    rsp = restore --staged --patch
    l = log --shortstat
    sw = show
